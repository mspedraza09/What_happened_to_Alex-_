extends "res://game/scripts/apps/BaseApp.gd"

# App Notas — se desbloquea en Fase 2+
# Contiene las notas diarias de Alex: qué tenía planeado hacer cada día.
# Función: guía indirecta; indica al jugador qué buscar y dónde.
# No genera pistas directamente — orienta la exploración.
# El contenido es estático en la escena (.tscn).

func _on_app_ready() -> void:
	pass  # Contenido narrativo estático, no requiere lógica adicional
